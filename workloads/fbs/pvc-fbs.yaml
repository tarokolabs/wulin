apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: default
  name: pvc-fbs
spec:
  storageClassName: standard
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 10Gi
