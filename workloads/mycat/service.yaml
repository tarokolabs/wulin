apiVersion: v1
kind: Service
metadata:
  name: svc-mycat
  labels:
    name: mycat
spec:
  type: NodePort
  externalTrafficPolicy: Local
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30090
  selector:
    app: mycat
