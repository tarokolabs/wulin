apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - datasource-config.yaml
  - deployment.yaml
  - service.yaml

# 統一為資源加上 label
labels:
  - pairs:
      app: grafana
